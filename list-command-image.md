- `docker image ls` = Untuk list image yang ada di docker kita.

- `docker image pull namaimage:tag` = Untuk download docker image.

- `docker image rm namaimage:tag` = Untuk menghapus docker image.

- 
